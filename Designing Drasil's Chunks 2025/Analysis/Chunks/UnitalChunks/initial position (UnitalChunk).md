```haskell
iPos = uc CP.iPos (sup lP initial) Real metre
```

A [[UnitalChunk]] extending [[initial position (ConceptChunk)]] with a display [[Symbol]] ($p_\text{initial}$), type information ([[Real]]), and units: [[Metre (UnitDefn)]].