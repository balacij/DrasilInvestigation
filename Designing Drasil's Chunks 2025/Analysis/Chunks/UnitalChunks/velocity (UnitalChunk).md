```haskell
velocity               = uc CP.velocity               (Concat [vec lV, label "(", lT, label ")"]) Real        velU
```

A [[UnitalChunk]] that extends [[velocity (ConceptChunk)]] with a symbol ($\vec{v}(t)$) and information that it represents a variable of type [[Real]] with [[velocity (UnitDefn)]] units.