```haskell
projPos :: UnitalChunk
projPos = uc C.projPos (Concat [lP, label "(", lT, label ")"]) Real metre
```

A [[UnitalChunk]] that extends [[1D position (ConceptChunk)]] with a display [[Symbol]] ($p(t)$), information that the variable is [[Real]]-typed, and is of unit: [[Metre (UnitDefn)]].
